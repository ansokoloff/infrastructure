---
 - hosts: 
   - localhost
   gather_facts: true
   become: yes
   roles:
   - docker

 - hosts: all
   gather_facts: true
   become: yes
   roles: 
   - common

 - hosts: localhost
   gather_facts: true
   become: yes
   roles:
   - jenkins

   
 - hosts: 
   - app
   - kuber
   gather_facts: true
   become: yes
   roles:
   - kubusers
   - installk8s

 - hosts: kuber
   gather_facts: true
   become: yes
   roles:
   - kuber
   - k8smaster
   - helm

 - hosts: app
   gather_facts: true
   roles:
   - k8sworker
 
