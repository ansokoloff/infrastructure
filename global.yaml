---
 - hosts: 
   - localhost
   - app
   gather_facts: true
   become: yes
   roles:
   - docker

 - hosts: all
   gather_facts: true
   become: yes
   roles: 
   - common

 - hosts: localhost
   gather_facts: true
   become: yes
   roles:
   - jenkins

   
 - hosts: 
   - app
   - kuber
   gather_facts: true
   become: yes
   roles:
   - users

 - hosts: kuber
   gather_facts: true
   become: yes
   roles:
   - k8s
   - helm

